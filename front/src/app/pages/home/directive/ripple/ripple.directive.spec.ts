import { RippleDirective } from './ripple.directive';

describe(RippleDirective.name, () => {
  it('should create an instance', () => {
    const directive = new RippleDirective();
    expect(directive).toBeTruthy();
  });
});
