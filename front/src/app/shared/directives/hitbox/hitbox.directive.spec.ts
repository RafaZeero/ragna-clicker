import { HitboxDirective } from './hitbox.directive';

describe('HitboxDirective', () => {
  it('should create an instance', () => {
    const directive = new HitboxDirective();
    expect(directive).toBeTruthy();
  });
});
